= B5 Documentation
:toc:
:toclevels: 3
:sectnums:
:stem: latexmath

== B5: Underflow and Near Underflow

Aharoni et al.

=== Description
This model creates a test-case for each of the following constraints on the intermediate results:

[lowerroman]

* A random positive SubNorm
* A random negative SubNorm
* All the numbers in the range latexmath:[[+MinSubNorm - 3 ulp, + MinSubNorm + 3 ulp] ]
* All the numbers in the range latexmath:[[-MinNorm - 3 ulp, -MinNorm + 3ulp] ]
* All numbers in the range latexmath:[[MinNorm - 3 ulp, MinNorm + 3 ulp] ]
* All numbers in the range latexmath:[[-MinNorm - 3 ulp, -MinNorm + 3 ulp]]
*A random number in the range latexmath:[(0, MinSubNorm)]
*A random number in the range latexmath:[(-MinSubNorm, -0)]
* One number for every exponent in the range latexmath:[[MinNorm.exp, MinNorm.exp + 5] ]

*Number of tests:*

*Precisions Supported:* `BF_16`, `FP_16`, `FP_32`, `FP_64`, `FP_128`

*Operations Supported:* `All`

== Implementation

=== Definitions
`min_exp`:: The minimum exponent for each precision, where the biased exponent value is 1.
`lp/hp`:: As specified within the operations section, this test suite will be performing high to low precision converts. `hp` refers to the higher precision input operand for these operations, `lp` refers to the lower precision output of the conversion.
`m_bits`:: The number of mantissa bits, dependent on the precision.
`min_sn_exp`:: The smallest possible subnormal value based on the precision, 2^min_exp - m_bits.
`min_sn`:: The smallest possible subnormal input, 2^(min_exp - m_bits)
`hp_exp/lp_exp`:: The exponent value for `hp` and `lp` respectively.
`lp_sn_exp`:: The exponent value corresponding to a subnormal value for `lp`. In any precision, this is 0 when represented as a biased exponent. In most tests, the mathematical operations will be expressed where `lp_sn_exp` is unbiased.
`lsb_exp`:: The exponent value for the least significant bit.
`lrs_int`:: The integer representation of the desired Isb, rounding, and guard bit values.

== Operations
=== One operand

* FP_128 -> FP_64 | FP_128 -> FP_32 | FP_128 → FP_16 | FP_128 → BF_16
* FP_64 -> FP_32 | FP_64 -> FP_16 | FP_64 -> FP_16|FP_64 -> BF_16
* FP_32 -> FP_16 | FP_32 -> BF_16
* FP_16 -> BF_16

=== Two operands
* Multiply
* Divide

=== Three operands
* FMADD
* FMSUB
* FNMSUB
* FNMADD

=== Rounding Modes

* Round Near Even
* Round To Min Mag
* Round To Min
* Round To Max
* Round Near Max Mag
* Round To Odd

=== General Implementation
Each test presents a different requirement on the result. Based on this requirement, each script determines the desired output exponent, sign, and mantissa based on the following information:

==== Tests iii/iv Mantissa Pattern

[cols="2*"]
|===
| Value  | Mantissa Pattern

| MinSubNorm + 3 ulp| 0000_0000_01 1*
| MinSubNorm + 2 ulp| 0000_0000_01 10
| MinSubNorm + 1 ulp| 0000_0000_01 0*
| MinSubNorm | 0000_0000_01 00
| MinSubNorm - 1 ulp | 0000_0000_00 1*
| MinSubNorm - 2 ulp| 0000_0000_00 10
| MinSubNorm - 3 ulp| 0000_0000_01 0*
|===


==== Tests v/vi Mantissa Pattern

[cols="2*"]
|===
| Value  | Mantissa Pattern

| MinNorm + 3 ulp| 0000_0000_00 1*
| MinNorm + 2 ulp| 0000_0000_00 10
| MinNorm + 1 ulp| 0000_0000_00 0*
| MinNorm | 0000_0000_00 00
| MinNorm - 1 ulp | 1111_1111_11 10
| MinNorm - 2 ulp| 1111_1111_11 0*
| MinNorm - 3 ulp| 1111_1111_11 00
|===

==== Desired Sign

[cols="2*"]
|===
| Test # | Sign

| i       | 0
| ii      | 1
| iii     | 0
| iv      | 1
| v       | 0
| vi      | 1
| vii     | 0
| viii    | 1
| ix      | *
|===

==== Determining lsb_exp

[cols="2*"]
|===
| Test #  | `lsb_exp`

| 1  | latexmath:[min\_exp - m\_bits]
| 2  | latexmath:[min\_exp - m\_bits - 1]
| 3  | latexmath:[min\_exp - m\_bits - 1]
| 4  | latexmath:[min\_exp - m\_bits]
|===

==== Desired Exponent

[cols="4*"]
|===
| Least Significant Bit  | Rounding Bit  | Sticky Bit | exp
| 1 | * | *  | latexmath:[lsb\_exp]
| 0  | 1  | *  | latexmath:[lsb\_exp - 1]
| 0  | 0  | 1  | latexmath:[<= lsb\_exp - 2]
|===
